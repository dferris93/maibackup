# Example config with a successful backup command.
BACKUP_CMD=(ls -l)
SLEEPTIME=1
LOGDIR="./logs"
LOGFILE="$LOGDIR/success.log"

pre_backup_command() {
    echo "pre backup command test"
    return 0
}

post_backup_command() {
    echo "post backup command test"
    return 0
}

failed_backup_command() {
    echo "failed backup command test"
    return 0
}

successful_backup_command() {
    echo "successful backup command test"
    return 0
}
